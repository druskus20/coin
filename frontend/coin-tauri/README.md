# Coin Tauri 

## Running with Tauri 

```bash
cargo tauri dev 
cargo tauri build 
```

Both of these commands will build and bundle the code in `../coin-web-ui` with npm (Vite).


## Running separate

```bash
cargo run 
cargo build 
```
